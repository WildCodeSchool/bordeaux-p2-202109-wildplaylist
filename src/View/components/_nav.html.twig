<nav class="navbar navbar-expand navbar-dark bg-secondary border-bottom border-primary border-bottom gradient-border">
    <div class="container-fluid position-relative">
        <div>
            <a class="navbar-brand ms-2" href="/">
                <i class="fas fa-home"></i>
            </a>
        </div>
        <div id="navbarSupportedContent">
            {% include 'components/_loginbox.html.twig' %}
            {% include 'components/_registerbox.html.twig' %}
            <ul class="navbar-nav">
                <h6 class="nav-item session-username align-self-center mt-2">{{ session.user.pseudo }}</h6>
                <li class="nav-item dropdown">
                    <a class="btn d-flex" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="https://avatars.githubusercontent.com/{% if (session.user) %}{{ session.user.github_name }}{% else %}github{% endif %}"
                             alt="logo github" width="30" height="auto" class="d-inline-block align-text-center rounded-circle">
                    </a>
                    {% if (session.user) %}
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown ">
                        <a class="dropdown-item" href="/user?id={{ session.user.id }}">Mon Compte</a>
                        <a class="dropdown-item" href="logout">Deconnexion</a>
                    </div>
                    {% else %}
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown ">
                        <a class="dropdown-item" href="" data-bs-toggle="modal" data-bs-target="#registerModal">S'inscrire</a>
                        <a class="dropdown-item" href="" data-bs-toggle="modal" data-bs-target="#loginModal">Se connecter</a>
                    </div>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>